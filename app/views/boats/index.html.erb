<h1>All Boats</h1>
<h2>Find the perfect boat for your adventure<h2>

<div class="jumbotron">

</div>

<wrapper>
  <container>
    <div class='col-sm-10 col-sm-offset-1'>
      <div class='flex-box'>
        <% @boats.first(6).each do |boat| %>
          <%= render "show_card", boat: boat %>
        <% end %>

        <%= render "show_card", boat: @boats.where('category = sailboat').first %>

      </div>
    </div>
  </container>
</wrapper>

